# Q2 运行记录

## 任务边界

- 放行令牌：`R-Q2-COLLISION-001`。
- 只执行 Q2：有向矩形碰撞、拓扑排除、外接圆初筛、SAT 精判、首次碰撞时间搜索与验证。
- 未执行 Q3 最小螺距、Q4 调头或 Q5 速度上限；未修改共有几何和 Q1 正文结论。

## 运行环境

- Windows 11；Python 3.12.4；SciPy 1.17.1；NumPy 2.x；openpyxl 3.1.5；Matplotlib 3.x。
- Q1 接口：`建模总控/01-shared/code/spiral_chain.py`，参数 `pitch=0.55`、`head_speed=1.0`、`n_handles=224`。
- Q2 代码：`code/collision_q2.py`、`code/solve_q2_collision.py`。
- 碰撞接触数值容差：`1e-10 m`；Brent 时间容差：`1e-10 s`。

## 运行条目

| 运行编号 | 命令 | 状态 | 证据 |
|---|---|---|---|
| RUN-Q2-TDD-001 | `python -m pytest .../test_collision_q2.py -q` | 通过 | 5 passed；接触边界、相邻排除、圆筛、旋转 SAT 与扫描辅助函数 |
| RUN-Q2-SCAN-001 | `python -X utf8 .../solve_q2_collision.py` | 通过 | 0.5 s 全扫描，885 个时刻，首次区间 412.0--412.5 s |
| RUN-Q2-REFINE-001 | 同一入口内 Brent 精化 | 通过 | (t=412.4738376821) s，(|G|<4e-15) m |
| RUN-Q2-STAB-001 | 同一入口内 1 s/2 s 复核 | 通过 | 精化时间差分别 (4.5e-13) s、(2.3e-13) s |
| RUN-Q2-PACK-001 | `python -X utf8 .../package_q2_deliverables.py` | 通过 | result2.xlsx、表格、图已生成 |
| RUN-Q2-DENSE-001 | `python -X utf8 .../validate_dense_precritical.py` | 通过 | 0.1 s 完整扫描 0--412.5 s；唯一正→非正区间 `[412.4,412.5]` s；0.001 s 末段锁定 `[412.473,412.474]` s |

## 关键结果

- 首次碰撞时间：412.4738376821 s。
- 首次候选板对：代码 `(0,8)`，论文 `(1,9)`。
- 临界时刻全局裕度：约 0 m；圆筛拒绝 23,272 对，SAT 精判 1,259 对。
- 非单调证据：扫描首次碰撞后在 436 s、439 s 附近出现新的安全—碰撞转变。
- 早期漏检证据：0--412 s 的 4,121 个 0.1 s 采样点全部 `G(t)>0`；最小采样裕度 `8.73218e-4 m @ 409.8 s`；67 个采样局部极小点中最小精化值 `8.65235e-4 m @ 409.8319894 s`；0.05 s 错位网格最小值 `8.67767e-4 m`；410--412.5 s 的 0.001 s 扫描仅发现 `[412.473,412.474]` s 跨越。

## 已知局限

1. 采用二维俯视有向矩形；不含三维遮挡、孔径和连接间隙。
2. Q1 的解析链状态作为上游接口；本问不重新拟合或替换 Q1。
3. `result2.xlsx` 按题目模板写入临界时刻位置和速度，并在副本中恢复中文表头与节点标签；原始模板未被覆盖。
4. 本轮同步更新 Word 审阅稿后，已完成段落结构回读；当前环境未安装 LibreOffice/soffice，未能完成新的页面 PNG 渲染，不能把 Word 版称为本轮视觉复核通过。
