# Result Card：Q2_COLLISION

- 放行令牌：`R-Q2-COLLISION-001`
- 采用模型：二维有向矩形；拓扑排除 `|i-j|=1` → 外接圆安全初筛 → SAT 精判；全局裕度 `G(t)=min g_ij(t)`；Brent 精化首次 `G(t)=0`。
- 关键公式：
  - `C_i=(P_{i-1}+P_i)/2`；
  - `ε_i=(P_i-P_{i-1})/||P_i-P_{i-1}||`；
  - `ρ_i(e)=a_i|ε_i·e|+0.15|n_i·e|`；
  - `g_ij(t)=max_e(|(C_j-C_i)·e|-ρ_i(e)-ρ_j(e))`。
- 运行命令：
  - `python -m pytest 建模总控/03-deliverables/02_Q2_碰撞模型/code/test_collision_q2.py 建模总控/tests/test_q1_full.py 建模总控/tests/test_spiral_chain.py -q`
  - `python -X utf8 建模总控/03-deliverables/02_Q2_碰撞模型/code/solve_q2_collision.py`
  - `python -X utf8 建模总控/03-deliverables/02_Q2_碰撞模型/code/package_q2_deliverables.py`
- 验证数字：18 tests passed；223 块板凳、24,531 组非相邻板对；0.5 s 扫描 0--442 s；首次区间 `[412.0,412.5]` s；`t*=412.4738376821 s`；`G(t*)=-3.8580e-15 m`；首次候选代码 `(0,8)`、论文 `(1,9)`；临界时刻圆筛拒绝 23,272 对、SAT 精判 1,259 对；1 s/2 s 复核时间差小于 `5e-13 s`。
- 追加漏检验证：0--412.5 s 完整 0.1 s 扫描共 4,126 点，唯一正→非正区间为 `[412.4,412.5] s`；0--412 s 全部采样 `G(t)>0`；最小采样裕度 `8.73218e-4 m @ 409.8 s`，局部极小值精化后为 `8.65235e-4 m @ 409.8319894 s`；0.05 s 错位网格最小值 `8.67767e-4 m`；0.001 s 末段扫描唯一跨越为 `[412.473,412.474] s`。
- 输出路径：`建模总控/03-deliverables/02_Q2_碰撞模型/`，包括 `README.md`、论文手审阅稿、验证表、候选板对表、`result2.xlsx`、CSV/JSON、图、日志和运行记录。
- 已知局限：二维俯视投影；不建模三维遮挡、孔径、连接间隙；Q1 的全量工作簿只到 300 s，本问沿同一已验证解析接口继续盘入至首次临界约 412.47 s；因此不能把结果解释为三维唯一物理先撞对象。
- 是否建议进入论文手审阅：**是**。主结果、候选板对、时间稳定性和独立多边形复核均已落盘；论文应采用系统级“至少一组非相邻板对发生碰撞”的谨慎表述。
