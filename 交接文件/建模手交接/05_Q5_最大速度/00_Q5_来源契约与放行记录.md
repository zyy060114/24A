# Q5 来源契约与总控放行记录

## 1. 放行信息

| 字段 | 内容 |
|---|---|
| 放行编号 | `R-Q5-P0450342-INTERFACE-002` |
| 来源总控线程 | `01a027af-8edd-78e3-ba3f-1c5b2a5491e6` |
| 当前范围 | Q5 来源契约、统一公式、可复现代码接口、Q4 依赖说明 |
| 报告对象 | 总控对话 |
| 用户联系 | 仅由总控统一汇报 |
| 继续派工 | 禁止 |
| Git 操作 | 不 commit、不 push |
| 当前状态 | 已接收 Q4 的 (p=0.450342\,\mathrm m) 严格2:1边界双圆弧基线并完成条件性Q5求解；Q4基线存在碰撞 |

## 2. Source-to-Task Contract

| 来源 | 必须保留的要求 | 禁止的简化或替换 | 当前验收证据 |
|---|---|---|---|
| 题面问题4 | 原始螺距为 (p=1.7\,\mathrm m) | 不得把 (0.450342\,\mathrm m) 写成题面原始参数 | README 与正文均显式区分两种口径 |
| 问题3正式结果 | 安全上界 (p_U=0.450341796875\,\mathrm m) | 不得继续采用已撤销的0.451 m口径 | 引用相对路径 `../../03_Q3_最小螺距/03_Q3_最小螺距_论文手审阅.md` |
| 用户参数更正 | Q5采用 (p=0.450342\,\mathrm m)，比安全上界高 (2.03125\times10^{-7}\,\mathrm m) | 不得把临界估计、安全上界和本次上取值混写 | 本契约与 `01_Q5_模型接口与等待依赖.md` |
| Q4当前基线 | 严格2:1、与内螺线外圆相切的S形双圆弧 | 不得改变半径比或省略相切条件 | Q4最终路径合同与几何验证文件 |
| 碰撞适用边界 | Q3不碰撞保证只覆盖内螺线 | 不得直接推出加弧后的Q4路径也不碰撞 | Q4必须单独提交完整路径SAT碰撞验证 |
| 公共速度模型 | 固定几何状态下使用刚杆约束微分递推 | 不得引入经验衰减率或假设速度沿龙身单调 | `code/q5_velocity_interface.py` 与单元测试 |
| Q4→Q5依赖 | 必须接收 Q4 最终分段路径、切点、圆心、半径、转向和弧长域 | 不得自行猜测或沿用未经总控确认的圆弧参数 | `tables/Q5_待接收字段表.md` |
| 正式 Q5 求解 | 求全部把手倍率，取全路径全节点最大值 | 不得只扫描整数秒或只看龙尾 | `tables/Q5_验证计划.md` |

## 3. 已完成工作

1. 统一 Q5 假设、符号和速度递推公式；
2. 接收 Q4 的 `point(s)`、`tangent(s)`、节点位置恢复与速度倍率接口；
3. 完成 -100 s 至 100 s 的0.25 s粗扫、峰值邻域0.01 s加密和224节点极值搜索；
4. 完成速度上限回代、中心差分复核和结果表生成。

## 4. 正式使用边界

1. 不得使用题面 (p=1.7\,\mathrm m) 的路径结果冒充本扩展场景；
2. 本问得到的是“当前固定几何基线上的速度约束上限”，不得写成全过程安全速度；
3. Q4已检出碰撞，降低速度只改变时间尺度，不能消除几何碰撞；
4. 若Q4更换切点或更换为无碰撞路径，必须重新计算Q5；
5. 未执行 commit、push、PR。

## 5. Q4依赖接收结论

Q4已提供 (p=0.450342\,\mathrm m) 的严格2:1边界双圆弧、任意路径状态的全部把手位置与单位切向接口。独立碰撞复核的结论不是“不碰撞”，而是201个整数时刻中88个状态碰撞。因此Q5数值可以作为速度模型的条件性结果，但不能作为满足几何与速度双重约束的最终可行方案。
