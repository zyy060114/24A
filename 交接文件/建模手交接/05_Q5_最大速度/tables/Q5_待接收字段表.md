# Q5 已接收的 Q4 路径字段

| 字段 | 符号或建议名称 | 单位 | 必需性 | 用途 |
|---|---|---:|---|---|
| 扩展场景螺距 | (p) / `pitch_m` | m | 必须等于0.450342 | 锁定场景口径 |
| Q3安全上界 | (p_U) / `q3_safe_upper_m` | m | 必须等于0.450341796875 | 证明本取值位于安全侧 |
| 双圆弧半径比 | (R_1:R_2) | — | 必须严格为2:1 | 锁定Q4基线结构 |
| 相切结构说明 | `tangency_contract` | — | 必须 | 证明双圆弧与规定螺线/圆相切 |
| 盘入螺线参数域 | `inbound_domain` | rad或m | 必须 | 定义完整路径范围 |
| 盘出螺线参数域 | `outbound_domain` | rad或m | 必须 | 定义完整路径范围 |
| 两个螺线切点 | (A,B) | m | 必须 | 分段路径连接 |
| 两段圆弧圆心 | (O_1,O_2) | m | 必须 | 圆弧点和切向计算 |
| 两段圆弧半径 | (R_1,R_2) | m | 必须 | 圆弧参数化 |
| 圆弧转向 | `turn_sign_1/2` | 无量纲 | 必须 | 确定单位切向方向 |
| 圆弧起止角 | `angle_start/end` | rad | 必须 | 确定有效弧段 |
| 完整路径累计弧长域 | ([s_{\min},s_{\max}]) | m | 必须 | Q5全路径扫描 |
| 路径点接口 | (\Gamma(s)) | m | 必须 | 节点位置恢复 |
| 单位切向接口 | (\boldsymbol\tau(s)) | 无量纲 | 必须 | 速度倍率递推 |
| 把手连续根规则 | `root_continuation_rule` | — | 必须 | 避免多交点分支跳跃 |
| Q4碰撞验证 | 最小SAT裕度及位置 | m | 已接收：最小裕度为负 | 说明本Q5数值仅为条件性速度上限 |

实际计算通过相邻目录 `../04_Q4_调头路径_p0450342扩展/code/` 的路径接口接收几何参数，没有从示意图反推圆弧。

## 建议Q4直接提供的文件

1. `Q4_p0450342_path_contract.json`：螺距、切点、圆心、半径、角域、转向、累计弧长域和版本号；
2. `Q4_p0450342_path.py`：统一的 `point(s)`、`tangent(s)`、连续节点恢复和分段边界接口；
3. `tables/Q4_p0450342_geometry.csv`：全部几何参数及相切残差；
4. `tables/Q4_p0450342_collision_validation.csv`：完整路径各扫描状态的最小SAT裕度、危险板对和连接点加密结果；
5. `Q4_p0450342_validation.md`：严格2:1、相切约束、连续性、碰撞安全和步长收敛结论。
