# 建模手交接索引

本目录集中存放第四问、第五问的建模手交接材料。论文手应先读各问题文件夹的主交接稿，再按相对路径查看结果表、验证证据和复现代码。

| 问题 | 入口 | 当前口径 |
| --- | --- | --- |
| Q4 | `04_Q4_调头路径_p0450342扩展/04_Q4_p0450342_建模手交接.md` | 用户指定的Q3→Q4扩展，$p=0.450342$ m；题面原Q4参数1.7 m另行区分 |
| Q5 | `05_Q5_最大速度/` | 读取Q4新交接包后计算；不得回退到0.451 |

统一结构：`本问目标 → 继承与新增 → 为什么这样建模 → 完整求解过程 → 结果及其实际含义 → 模型检验/局限/下一问接口`。每份主交接稿必须包含文字箭头流程和建模模块表。

`../_superseded_04_Q4_调头路径_p0451扩展/` 是已被0.450342替代的历史草稿，仅用于追溯，不得作为论文或后续计算输入。
