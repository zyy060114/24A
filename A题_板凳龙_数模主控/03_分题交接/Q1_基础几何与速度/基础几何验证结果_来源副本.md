# 公共几何模型验证记录

## 验证边界

本记录只验证公共几何模型：初始位置、相邻把手中心距、节点参数顺序、最近有效交点和实体矩形恢复接口。不验证问题1速度，也不运行问题2--问题5。

## 自动验证

- 共享几何测试：`python -m pytest 建模总控/tests/test_spiral_chain.py -q`
- 通过结果：7 passed
- 覆盖内容：初始坐标、相邻孔距、弧长位置接口、节点参数单调性、最近有效根和公共状态接口。

## 几何验收口径

| 检查项 | 判据 |
| --- | --- |
| 龙头初始点 | (P_0(0)=(8.8,0)) m |
| 龙头相邻把手中心距 | (2.86) m |
| 其余相邻把手中心距 | (1.65) m |
| 节点顺序 | 沿已选轨迹分支连续递推，不跳圈 |
| 定长圆求交 | 取最近有效交点，不用局部圆弧替代真实轨迹 |
| 实体恢复 | 每块板凳由两个相邻节点确定中心、方向和四角点 |

## 结论

公共几何模型的输入、递推和实体恢复接口已分离完成，问题1速度变量不再混入本公共模型交付物。后续问题只继承这些几何接口，并在各自文件夹中增加新的模型层。
